import 'package:flutter/material.dart';

class CardType2Screen extends StatelessWidget {
  const CardType2Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Otras tarjetas'),
      ),
      body: const Center(
        child: Text('CardType2Screen'),
      ),
    );
  }
}
